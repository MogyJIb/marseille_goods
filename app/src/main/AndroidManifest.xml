<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.gomel.marseille.goods"
    >

    <application
        android:name=".MarseilleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme"
    >

        <activity
            android:name="by.gomel.marseille.goods.presentation.splash.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Splash"
        >

            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                />
                <category
                    android:name="android.intent.category.LAUNCHER"
                />
            </intent-filter>

        </activity>

        <activity
            android:name="by.gomel.marseille.goods.presentation.MainActivity"
            android:windowSoftInputMode="stateHidden|stateAlwaysHidden|adjustResize"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout"
        />

    </application>


</manifest>
